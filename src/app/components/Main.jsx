import React from 'react'
import styled from '@emotion/styled'

const AppStyleWrapper = styled.div`
  height: 100vh;
  width: 100vw;
  overflow: hidden;
  display: flex;
`

const Main = () => (
  <AppStyleWrapper>
        WORKS!
  </AppStyleWrapper>
)

export default Main
